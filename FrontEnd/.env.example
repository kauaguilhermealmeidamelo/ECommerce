VITE_API_URL=http://localhost:8000/api

# Prefixo único e não-óbvio por cliente — NÃO substitui autenticação,
# só evita que alguém encontre o painel chutando /admin na URL.
VITE_ADMIN_PATH=painel-9x4k2
